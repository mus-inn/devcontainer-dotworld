services:
  ##APP_NAME##:
    image: alpine:latest
    container_name: ##APP_NAME##
    volumes:
      - '${APP_VOLUME}:/workspaces/${APP_NAME}'
      - '/var/run/docker.sock:/var/run/docker.sock'
    networks:
      - infrastructure 
networks: 
  infrastructure:
    driver: bridge

